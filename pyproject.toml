[build-system]
requires = ["setuptools>=64", "setuptools_scm[toml]>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "fmri-fm"
dynamic = ["version"]
description = "MedARC fMRI foundation model"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets",
    "einops",
    "huggingface-hub[hf-transfer]",
    "ipython",
    "jaxtyping",
    "jupyter",
    "matplotlib",
    "nibabel",
    "nsdcode",
    "numpy",
    "omegaconf",
    "pre-commit",
    "pytest",
    "pycortex",
    "scikit-learn",
    "scipy",
    "timm",
    "torch==2.8.0",
    "torchvision",
    "tqdm",
    "pydantic>=2.9",
    "wandb",
    "webdataset",
    "cloudpathlib[s3]",
    "lightning-sdk",
]

[project.optional-dependencies]

eval = [
    "fmri-fm-eval"
]

[tool.setuptools_scm]
version_file = "src/_version.py"

[tool.uv.sources]
fmri-fm-eval = { git = "https://github.com/medarc-ai/fmri-fm-eval.git", rev = "b114b89e68c6a046086c91ef191cdceddb5f2a18" }
nsdcode = { git = "https://github.com/cvnlab/nsdcode.git", rev = "bfd36a503cc90a3eb3ebfd69b269403f7e186924" }

[tool.ruff]
line-length = 100

[tool.ruff.lint]
ignore = ["F722"]
